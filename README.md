# Reinventing-TorchGrad

PyTorch の 自動微分アルゴリズムの再現実装

## 環境構築

MacOS, Linux

```
python -m venv venv
source venv/bin/activate
pip install -e .
```

Windows

```
python -m venv venv
venv\Scripts\activate
pip install -e .
```
